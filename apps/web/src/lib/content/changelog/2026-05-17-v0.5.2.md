---
version: v0.5.2
fecha: 2026-05-17
titulo: Parche — Páginas estáticas prerenderizadas
---

Las páginas de contenido estático ahora se prerrenderizan en tiempo de build, eliminando llamadas al servidor en cada visita.

## Cambios

- `/cambios` — convertida a carga universal (`+page.ts`) con `prerender = true`; lee los archivos Markdown en build time
- `/plan` — `prerender = true`; contenido 100% estático
- `/acerca` — `prerender = true`; contenido 100% estático
